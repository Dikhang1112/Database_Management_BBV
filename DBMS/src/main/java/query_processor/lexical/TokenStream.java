package query_processor.lexical;

/**
 * Đóng gói chuỗi các Token (TokenStream) phục vụ cho giai đoạn Parsing.
 */
public class TokenStream {

    /**
     * Khởi tạo đối tượng TokenStream.
     */
    public TokenStream() {
        // TODO: Future DBMS logic implementation
    }

    /**
     * Lấy token tiếp theo trong luồng.
     *
     * @return Đối tượng Token tiếp theo.
     */
    public Token nextToken() {
        // TODO: Future DBMS logic implementation
        return null;
    }

    /**
     * Kiểm tra luồng còn token chưa xử lý hay không.
     *
     * @return true nếu còn token.
     */
    public boolean hasNext() {
        // TODO: Future DBMS logic implementation
        return false;
    }
}
