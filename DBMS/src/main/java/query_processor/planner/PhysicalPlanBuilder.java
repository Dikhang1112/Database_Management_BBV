package query_processor.planner;

/**
 * Xây dựng từng bước cây Kế hoạch Thực thi Vật lý (PhysicalPlan) từ LogicalPlan.
 * Pattern: Builder Pattern.
 */
public class PhysicalPlanBuilder {

    /**
     * Khởi tạo PhysicalPlanBuilder.
     */
    public PhysicalPlanBuilder() {
        // TODO: Future DBMS logic implementation
    }

    /**
     * Build đối tượng PhysicalPlan từ Kế hoạch Logic.
     *
     * @param plan Kế hoạch logic LogicalPlan.
     * @return Đối tượng PhysicalPlan.
     */
    public PhysicalPlan build(LogicalPlan plan) {
        // TODO: Future DBMS logic implementation
        return null;
    }
}
